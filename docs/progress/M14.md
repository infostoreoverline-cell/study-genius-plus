# M14 - Qualità e Prezzi misurati

## Stato
Completato.

## Funziona davvero
- **Evaluation Engine:** Creazione di `packages/evals/src/benchmark.ts` che esegue suite di test e produce uno score (da 0 a 4) per campioni didattici.
- **Pricing Simulator:** Implementato in `packages/evals/src/pricing.ts` il database dei prezzi ufficiali (Gemini Flash-Lite, Gemini Flash, DeepSeek Flash) e la logica di calcolo del costo esatto in microEUR tenendo conto del cambio valutario USD/EUR e CNY/EUR.
- **Profili Contrattualizzati:** Aggiornato `packages/contracts/src/providers.ts` per supportare il `pricingProfile` (economy, balanced, quality) da usare per la discriminazione del workload.

## Test eseguiti
- `npm run test:unit`: Verificati 3 test in `packages/evals/tests/eval.test.ts`.
  - Hanno verificato che la conversione e il calcolo dei costi delle tariffe Gemini e DeepSeek avvengano correttamente seguendo la matematica indicata in `09_Budget_e_costi.md`.
  - Hanno verificato che il runner produca un Evaluation Result integro.

## Cosa resta aperto
- Integrare l'Evaluation Engine con gli adapter reali e chiavi reali (se si desidera fare test live invece che test simulati).
- Adattamento della logica frontend per mostrare un preventivo usando la classe Pricing.

## Prossima Milestone
- M15 ("M15 - UI: Sessione di Studio") può iniziare, che sposta l'attenzione sull'esperienza utente e sull'interfaccia.
