# M01 — Contratti e archivio

Stato: completata
Contratti e ADR coinvolti: 04_Contratti_e_database.md
Comportamento raggiungibile dalla UI: N/A (solo strato dati)

## File e moduli modificati:
- `packages/contracts/src/entities.ts` (Schemi Zod base ed entity di dominio)
- `packages/contracts/src/requests.ts` (Schemi per GenerationRequest)
- `packages/contracts/src/reports.ts` (Schemi per ReviewReport e Findings)
- `packages/contracts/src/index.ts` (Barrel file)
- `packages/storage/src/migrations/001_initial_schema.sql` (Schema SQLite con vincoli relazionali)
- `packages/storage/src/database.ts`, `migrator.ts` (Gestione connessione e migratore versionato locale)
- `packages/storage/src/blob_store.ts` (Content-addressable blob store)
- `tests/integration/storage/*.test.ts` (Test di integrazione)

## Verifiche realmente eseguite:
- **Test migrazioni idempotenti**: eseguite transazioni SQLite e script di auto-migrazione, verificate chiamate duplicate.
- **Test concorrenza e atomicità**: testati rollback automatici su transazioni fallite, compresi inserimenti in conflitto tra loro per assicurare validazioni di Unique Constraints (base per i 409).
- **Test blob store**: testata scrittura e lettura per hash sha256 via filesystem per il recupero dei documenti importati.
- Tutti e 3 i test suite integration nativi per `node:test` hanno riportato esito positivo.

## Esiti e artifact dimostrativi:
- Database test SQLite `test-concurrency.db` temporaneo creato, manipolato e rimosso a fine esecuzione con totale coerenza referenziale garantita.
- Schemi SQL generati che implementano fedelmente i requisiti `04_Contratti_e_database.md` in pure SQL con Foreign Keys attive (`PRAGMA foreign_keys = ON`) per isolamento.

## Costo API delle prove, se presenti:
0 € (nessuna chiamata esterna per il livello dati locale).

## Limiti o problemi aperti:
Nessun blocco particolare sullo schema o storage manager; Node.js 24 LTS e TypeScript ESM con il native test runner si sono dimostrati molto veloci. Un deprecation warning su `fs.rmdir` rilevato nei test (può essere fixato in M02 sostituendolo con `fs.rm`).

## Prossimo blocco e dipendenze soddisfatte:
M02 è pronta per iniziare (Avvio locale, UI base, sessione locale). I contratti di persistenza sono tutti saldi.
